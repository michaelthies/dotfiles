# Repo to hold my custom config files

  - bash login scripts (.bash_profile)
  - console input settings (.inputrc, .editrc)
  - ruby/rails setup (.irbrc, .railsrc, .rdebugrc)
  - vim setup (.vim, .vimrc)
  - etc...
